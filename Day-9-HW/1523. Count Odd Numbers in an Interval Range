//1523. Count Odd Numbers in an Interval Range

class Solution {
    public int countOdds(int low, int high) {
        // The number of odd numbers in [0, n] is (n + 1) / 2
        return (high + 1) / 2 - low / 2;
    }
}
